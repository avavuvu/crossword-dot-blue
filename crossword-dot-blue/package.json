{
    "name": "crossword-dot-blue",
    "type": "module",
    "scripts": {
        "bindings": "cargo test -q -p crossword-tools -p crossword-dot-blue export_bindings",
        "bindings:check": "bun run bindings && git diff --exit-code -- ../crossword-tools/bindings && test -z \"$(git ls-files --others --exclude-standard -- ../crossword-tools/bindings)\"",
        "build": "vite build",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "dev": "concurrently -n vite,dx -c cyan,yellow \"vite build --watch\" \"dx serve --hot-patch --features local --package crossword-dot-blue\"",
        "fonts": "sh scripts/fonts.sh",
        "test": "bun test"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "concurrently": "^10.0.5",
        "typescript": "^5.0.0",
        "vite": "^8.3.0"
    }
}
